# 病人康复系统 Spec Changelog

## 0.21 - 2026-08-21

- 覆盖旧游戏设计中“不新增逐题模型、不展示每轮明细”的限制：六款认知游戏新增正式逐题子记录，`TrainingRecord` 继续代表整场训练，不新增独立 `GameSession`。
- 每个已完成题目记录排除展示、语音、暂停和反馈后的有效作答时间与正确性；整场结束时未完成题直接丢弃；拼图额外记录实际交换次数。
- 游戏训练新增客户端会话 UUID 和整场批量上传幂等语义；合法历史 `raw_detail.rounds` 转换为逐题数据，异常项跳过且原始 JSON 保持不变。
- 医生端沿用训练追踪详情，支持近 7 天、近 30 天和自定义日期的单场逐题明细及长期正确率、平均/中位作答时间趋势；拼图不进入综合正确率。

## 0.20 - 2026-08-20

- 五个正式运动动作统一使用七牛私有正式教学视频，并进入录像、上传、训练记录和医生审阅闭环。
- 真实患者单次运动录像上限为 30 分钟；审核演示统一为 10 分钟无录像、无上传、无持久化体验。
- AI 分析器按动作逐步注册；本期仅肩部推举支持动作分析，其余四个动作先积累标准录像和训练数据。

## 0.19 - 2026-08-20

- 确认 miwitracker `9018` 为平台级“寻找设备 / 响铃”指令，不依赖设备型号或患者绑定状态。
- 响铃成为停用设备唯一允许的远程命令例外；通信测试、主动测量、配置与同步继续遵守停用规则。
- 设备管理列表仅为 miwitracker 设备显示响铃操作；成功或排队提示已下发，离线、超时和失败展示准确反馈。

## 0.18 - 2026-08-20

- 补齐微信小程序 `8888` 演示肩部推举的原流程：动作说明页恢复“动作预览”，独立预览页从固定演示计划加载正式肩部推举示范视频，不访问真实患者接口。
- 演示摄像训练页恢复可收起、可恢复的示范指导小窗，并与独立预览页复用同一视频；演示分支仍不录制、不生成文件、不上传、不持久化。

## 0.17 - 2026-08-20

- 微信小程序 `8888` 演示运动计划在原六个游戏后追加肩部推举，沿用“动作说明 → 摄像头训练”的正式入口流程。
- 演示肩部推举只使用前置摄像头预览和 1 分钟本地计时，支持提前结束、后台暂停与授权恢复；不录制、不生成视频、不上传、不持久化，也不更新周进度。
- 正式用户的肩部推举分段录像、缓存恢复、上传与动作分析流程保持不变。

## 0.16 - 2026-08-10

- “训练与健康”及其训练视频、动作分析、穿戴健康读取范围改为所有医生和管理员可查看全部已入组患者。
- 未加入任何项目的全局患者仍不进入“训练与健康”；患者小程序、设备管理及其他模块权限保持不变。
- 保留原主治医生、项目创建人和入组创建人行级过滤策略，通过服务端配置开关供后续权限迭代恢复。

## 0.15 - 2026-08-09

- 穿戴设备管理权限改为所有医生和管理员均可管理任意已入组患者，不再复用训练数据权限。
- “设备台账”更名为“设备管理”；新增设备只录入 15 位 IMEI，其余身份字段和四位固定简码由服务端生成。
- 通信测试可为新设备自动识别并保存型号；跨厂商重复 IMEI 被拒绝，历史非 IMEI 标识不再误显示为 IMEI。

## 0.14 - 2026-08-06

- 医生端“患者接入”保留外层标题，内层统一为“患者绑定信息”；小程序临时绑定码与穿戴设备操作收敛到同一右上角操作区。
- 穿戴设备改为通过四位数字简码 Modal 绑定；加载期间不提供绑定入口，已绑定设备不能直接替换，仍需先解绑。
- 统一 `Descriptions` 表格展示设备简码、设备 ID 与设备绑定时间，并保留患者切换时的状态隔离和迟到响应保护。
- 本次仅调整前端医生端布局与交互，不涉及后端接口、模型、迁移或依赖变更。

## 0.13 - 2026-08-05

- 覆盖 0.12 的客户端压缩决策：肩部推举保持低分辨率录制，改为每 15 秒生成原始 MP4 分段并直接上传，不再调用 `compressVideo`。
- 正常录像分段不再通过 `saveFile` 持久化；仅在上传失败时尝试保存失败分段，避免微信约 200MB 持久化文件空间被原始与压缩副本耗尽。
- 取消小程序端 50MB 分段拦截；服务端保留 80MB 单请求安全上限，完整视频继续不设置累计字节上限。
- 40 分钟录像对应 160 个标准分段，服务端最多允许 200 段，为暂停产生的短尾段保留余量。
- 兼容已有待压缩、压缩失败和已压缩清单；升级后旧原始分段直接转为待上传，不要求患者重新录像。
- 不升级现有依赖、基础镜像或运行环境。

## 0.12 - 2026-08-05

- 肩部推举默认改为微信相机低分辨率档位，并在小程序端把每个录像分段压缩到最高 720P、约 2Mbps、24fps 后再上传。
- 训练视频单段上限从 32MB 调整为 50MB；删除完整视频累计大小上限，最长录像调整为 40 分钟，最多 80 个分段。
- 明确压缩前先持久化原始分段；压缩失败保留原始文件、暂停上传并允许重试，禁止直接上传未压缩分段。
- 患者首页取得的当前处方写入进程内共享缓存；处方页优先即时展示缓存并后台刷新，不再清空列表等待重复网络请求。
- 放宽长视频动作分析下载与 stale 时限，不升级现有依赖、基础镜像或运行环境。

## 0.11 - 2026-07-24

- 覆盖旧设计中“不记录项目完结时间”的限制：新增 `StudyProject.completed_at`，由项目完结动作首次写入并保持幂等。
- 明确项目患者的穿戴研究窗口为 `[enrolled_at, completed_at)`；原始点按精确时间裁剪，日汇总与项目/分组聚合仅统计完整落入窗口的 `Asia/Shanghai` 自然日。
- 明确项目完结不解绑设备、不停止同步且不删除历史数据；完结后新健康数据继续进入全局患者档案，但不进入已完结项目分析。

## 0.10 - 2026-07-23

- 覆盖旧决策“真实设备同步后置”：本期接入厂商 OpenAPI，设备继续向厂商平台上报，MotionCare 不接 TCP 或 Webhook。
- 覆盖旧决策“手工录入每日健康数据”：删除 Web 与微信小程序健康填报，健康数据仅来自穿戴设备同步。
- 明确设备使用固定四位简码绑定全局患者，绑定历史按开始/解绑时间隔离患者数据。
- 明确保留心率、血压、血氧原始测量点，并按患者和 `Asia/Shanghai` 自然日生成日汇总；日内图表动态聚合。
- 明确每天凌晨 03:00 同步，按设备与指标历史动态补拉，每次最多回看七天。
- 明确厂商整日步数在同日换绑时无法拆分，必须标记不可归属并排除，不能猜测或分摊。
- 将“训练追踪”升级为一级类目“训练与健康”，患者详情拆成“训练跟踪/穿戴健康”两个页签，页签名不展示设备在线标签。
- 明确需求方确认的 CRF 不包含健康信息；本次穿戴接入不得修改 CRF 模板、registry、预览、导出或缺失校验。

## 0.9 - 2026-07-14

- 纠正 0.8 中“小程序直传七牛”的错误记录：小程序每 30 秒生成分片并上传 MotionCare 业务服务器，业务服务器通过任务系统合并完整视频后再上传七牛。
- 明确七牛只保存最终完整视频，不接收训练分片；七牛上传成功后业务服务器删除全部视频临时文件。
- 明确分片上传失败不打断后续录像，失败分片进入手机本地重试队列；手机端在分片上传确认后立即删除对应本地文件。
- 明确训练总时长不设硬上限；页面隐藏时自动结束训练并进入收尾。
- 新增视频处理任务状态、幂等合并、失败重试与 48 小时临时文件保留/过期清理规则。
- 明确完整视频上传七牛、校验、重试和服务端清理由后台任务自动完成，患者端与医生端不提供任务管理页面。
- 视频任务增加单实例互斥、Celery 延迟确认、陈旧任务自动重投和运行中任务过期保护；患者端增加页面隐藏收尾、确认后本地清理恢复与异常启动恢复。

## 0.9 - 2026-07-11

- 覆盖 0.8 的“肩部推举视频由小程序单文件直传七牛”决策：微信小程序改为约 30 秒分段录制，在训练过程中逐段上传 MotionCare 单台业务服务器。
- 明确服务器使用 FFmpeg 合并任意合理数量的分段，优先无损转封装，不兼容时最多执行一次完整转码；不调用七牛 `avconcat` 或其它多媒体处理服务。
- 明确服务器只把最终 MP4 上传一次七牛；七牛对象校验和训练记录创建成功后立即删除本地文件，失败文件最多保留 24 小时。
- 明确强制上传页只等待所有分段安全到达服务器并成功提交处理任务，不要求患者等待 FFmpeg、七牛上传和 PP-TinyPose 分析完成。
- 明确一期按 Django Web 与 Celery/FFmpeg Worker 共享本机临时磁盘的单机部署设计，多机共享暂存不在本期范围。
- 增加七牛 attempt 隔离、canonical 发布和长期清理 tombstone，确保解绑、任务接管和迟到上传不会留下不可追踪对象。
- 增加录像尾段失败闭环、600 秒安全停止、FFmpeg/FFprobe 启动健康检查及 staging 0700 权限要求。

## 0.8 - 2026-07-08

- 覆盖旧决策“视频上传、AI 动作识别后置”：本里程碑先将肩部推举升级为小程序录像跟练、七牛 Kodo 直传、医生端视频审阅和手动触发动作分析。
- 明确肩部推举继续沿用 `ActionLibraryItem -> PrescriptionAction -> TrainingRecord` 主链路，不新建独立运动训练体系。
- 明确视频上传采用七牛 Kodo 上传凭证和小程序直传；后端只生成凭证、校验绑定、创建训练记录和生成私有下载 URL。
- 明确 PP-TinyPose 仅负责人体关键点检测，肩部推举计数和标准/不标准判定由 MotionCare 规则层完成。

## 0.7 - 2026-05-16

- 覆盖旧决策“真实游戏后置”：本里程碑开始在微信小程序落地两款真实认知小游戏（颜色顺序记忆、反应抑制能力训练）。
- 明确真实游戏一期继续沿用 `ActionLibraryItem -> PrescriptionAction -> TrainingRecord` 主链路，不新增 `GameSession` 后端模型。
- 明确小程序游戏需达到患者真实使用体验：适老化界面、海南康复主题、语音引导、音效、暂停/继续、提前结束、自动上传和结果页。
- 明确网络失败补传策略：缓存一条待上传训练记录；单次打开周期退避式重试最多 10 次；超过后下次打开小程序重新启动一轮 10 次重试。

## 0.6 - 2026-05-14

- 明确「研究录入」改为项目患者维度：一行代表一个患者在一个项目中的入组关系，同一患者多项目按项目拆分。
- 新增项目患者研究录入页设计：顶部 T0/T1/T2 Tab，显示时间说明；草稿可编辑，已完成只读查看。
- 覆盖旧访视编辑口径：完成态访视不再允许继续编辑，草稿态承担可编辑流程。
- 将「CRF 基线」用户可见文案改为「基线资料」，页面标题为「患者基础基线资料」；该资料仍为患者维度一人一份，不包含 T0 评估。
- 明确患者详情去掉「打开 CRF」入口，CRF 入口保留在项目相关语境。

## 0.5 - 2026-05-12

- 建立项目根 **`AGENTS.md`** 作为 AI 协作宪法，覆盖 Cursor / Codex / Claude Code / Gemini CLI 等所有 AI 编程工具的共享指令。
- 创建 `CLAUDE.md`、`GEMINI.md` 软链接到 `AGENTS.md`，确保不同工具读到同一份"项目宪法"。
- 新增 `docs/superpowers/README.md`，作为 superpowers 工作流 spec / plan 的总索引，并明确每份 spec/plan 的状态（draft/approved/implementing/implemented/superseded）。
- 修订 `specs/patient-rehab-system/README.md`：把 `feature-list-for-client-quote.md` 加入索引；明确本目录与 `docs/superpowers/` 的职责边界；标注 `visuals/` 当前为空；补充工作流衔接说明。
- 给已落地的 plan（`drop-batch-concept.md` / `post-drop-batch-consolidation-execution-plan.md`）和被替代的 plan（`patient-project-admin-and-grouping-board.md`）补充顶部状态徽章（implemented / superseded），便于任何工具一眼识别"不能回退"。
- 明确**工具切换协议**：会话切换前必须 WIP commit；进入工具时必须先 `git status` + `git log` 理解上下文；不在用户确认前覆盖另一个工具留下的改动。

## 0.4 - 2026-05-11

- 明确"患者-项目绑定关系"仅在"分组内被确认"时才形成；删除一切"加入项目"的中间语义。
- "患者池"明确为虚拟概念：表达"所有患者"，不建模为实体；项目分组看板左侧的"池"是计算视图。
- **彻底删除批次概念**（`GroupingBatch` 模型/字段/API/前端状态全部删除）。
- 重新随机时，未确认（pending）的 ProjectPatient 会与池里勾选的患者一起再次随机；已确认者保持不变。
- 已确认患者只能"从本项目移除"（解绑），解绑后回到池；在组内列表中**置灰**展示（不隐藏）。
- 患者详情"加入研究项目"快捷入口改为"直接确认入组到分组"：必须指定 `group_id`，提交即为 confirmed。

## 0.3 - 2026-05-09

- 架构设计稿补充「本地 Vite + Django」场景下的 **CSRF 信任源（`CSRF_TRUSTED_ORIGINS` / `DJANGO_CSRF_TRUSTED_ORIGINS`）** 说明，与 `backend/config/settings.py` 实现保持一致，避免本地开发反复出现 `Origin checking failed`。

## 0.2 - 2026-05-06

- 明确第一版为医院 Web 后台研究数据闭环版。
- 明确小程序、真实设备、真实游戏、视频上传、AI 动作识别后置。
- 明确患者是全局基础档案。
- 明确项目、分组、患者解耦。
- 明确随机分组确认前可调整，确认后锁定。
- 明确处方版本化、动作快照、当前生效处方训练录入规则。
- 明确健康日数据手动录入。
- 明确 CRF 可带缺失导出。

- 2026-08-06：肩部推举视频训练记录手机端开始/结束时间；医生端视频抽屉按训练时段展示已同步的心率、血压、血氧趋势及心率、血压统计；步数和主动同步不在一期范围。
- 2026-08-07：训练视频健康观察窗口改为手机首次录像时间起、处方时长快照加 5 分钟止；实际完成时间保留为可选审计字段，不再影响穿戴趋势。

## 0.22 - 2026-08-30

- 微信小程序身份恢复代码落地：后端以一次性 `wx.login` code 调用微信 `code2Session`，客户端不再声明可信 OpenID，也不保留 `dev-openid` 回退或旧请求合同。
- 新增 OpenID 与 `ProjectPatient` 的持久一对一绑定；缓存清除、重装、更换设备或 token 过期后，可基于持久微信关系重新签发患者端会话。
- 兼容有效旧 token 的静默迁移；换绑只替换患者端微信关系与 token，不删除项目入组、处方、训练或 CRF 数据。
- 医生端以持久微信绑定判断患者接入状态，即使活动 token 已过期仍可识别并撤销绑定。
- 代码与数据库验证已完成；本条仅记录代码落地，不代表线上生效。生产发布仍需外部密钥系统注入指定 AppID 与非空 AppSecret，小程序基线独立 TypeScript 门禁待另案修复。

## 0.23 - 2026-09-02

- 六款认知游戏完成适老化 UI 收口：顺序记忆改为单项大图与 500ms 项间提示，选项增加逐项对错反馈，拼图仅保留选中边框，声音辨别按试听、盖牌和答题结果控制翻牌。
- 五个正式运动动作说明增加小米 MiMo 预生成本地语音，进入说明页自动播放一次并支持重播；播放失败不阻塞文字阅读、动作预览或训练。
- 认知游戏与动作说明语音代码已合并至 `main`；微信开发版 `7.0.3` 已上传并完成用户体验验收。

## 0.24 - 2026-09-02

- 微信小程序包体优化决策：18 张固定游戏图片和 5 段预生成动作说明语音改为使用稳定公开 HTTPS CDN URL；37 段游戏音频继续留在游戏分包，保证声音辨别和反馈不增加运行时网络前置依赖。
- 动作说明语音仍使用已验收的小米 MiMo M4A，保留自动播放、手动重播、页面生命周期清理和失败时文字降级；本条仅覆盖 0.23 中“本地内置”的存储方式。
- 固定素材不使用 `saveFile` 持久化缓存，避免影响肩部推举录像空间清理；图片型游戏仅在本次所需图片全部预取成功后才允许开始。

## 0.25 - 2026-09-02

- 微信小程序固定素材 CDN 优化已在隔离分支完成本地代码落地：23 项素材生成与本地校验、动作说明语音 CDN 接线、游戏图片全量预取门禁和包体预算脚本均已通过自动化验证。
- 使用示例 HTTPS API/CDN 构建变量执行生产微信构建（未请求 CDN），实测主包 `490570` 字节、`pages/game-session` 分包 `1033774` 字节、总包 `1524344` 字节；构建产物不含 18 张游戏图片、5 段动作说明 M4A 或七牛凭据，仍包含 37 段游戏 M4A 与 2 段公共网络告警 M4A。
- 本条仅记录本地代码与构建验证，不代表外部发布完成；真实七牛上传、正式 CDN 正文/媒体类型/不可变缓存头探测、微信合法域名、开发者工具依赖分析、iOS/Android 真机及微信版本上传均待用户授权和验收。
## 0.26 - 2026-09-04

- 肩部推举 v2 覆盖原“400 ms 内匹配后取左右事件并集”的计数语义：左右侧继续独立检测，最终 `total_count` 固定取左右事件数的较大值，动作明细以事件数更多、再按平均可靠覆盖率、再固定左侧确定的锚点侧生成；另一侧只做单调一对一质量匹配，峰值差不超过 400 ms 时直接匹配，401–800 ms 时要求双方为正时长且区间重叠至少 50%，800 ms 与 50% 边界包含在内。首次真实视频验收因旧并集合并把三档各 90 / 90 个左右事件计为 113 / 108 / 118 次而回退；新语义实现并复审后，第二次 5 FPS / 10 FPS / 全帧均为 90 次，计数、耗时、RSS 和 Swap 门槛全部通过。独立算法服务器正式 app 已切换到最终 v2，v1 previous、首次失败 v2 和历史脱敏报告均保留；服务器物理内存仍约 1.58 GiB，尚未达到 4 GiB 生产规格，生产队列接入仍需另行授权。

## 0.27 - 2026-09-04

- 肩部推举 v2 最终分支审查完成三项收口：历史 `shoulder-press-v1` 固定使用 5 FPS，`shoulder-press-v2` 才跟随全局采样配置；三档 benchmark 强制 `result.total_count`、`count_error` 与 `manual_total_count` 严格自洽，字段损坏时 fail-closed；动作明细与 tempo 严格使用锚点侧时间，幅度和肘角只由可靠覆盖率不低于 80% 的参与侧判定，低可靠侧只贡献 `low_confidence`。对应修复提交为 `b396c25`、`ebfb832`、`0df937f`。
- 使用 committed HEAD `0df937f03cfbdfb315c721bb1a90f4062a7e3df8` 重新生成并核验归档（SHA-256 `b72c5cf0f7de82659272c051e3bf5d979199dbb39be48857e00b2f4035df1551`），以 run_id `20260904T081815Z` 完成第三次前台串行验收：5 FPS / 10 FPS / 全帧均为 90 次、误差 0，耗时 183.176 / 225.758 / 421.844 秒，峰值 RSS 641,409,024 / 641,277,952 / 623,902,720 B，Swap 均为 0，`acceptance.passed=true`。
- 独立算法服务器正式 app 已更新为最终 `0df937f`；上一版通过的 `de56352` 保留为 `app.previous-de56352`，v1 `app.previous-77b0166`、首次失败 v2 和全部历史脱敏报告均未覆盖或删除。input/tmp 完全为空，无 benchmark、Celery 或 Web 进程；生产 PostgreSQL、Redis 与 Celery 仍未接入。服务器物理内存为 1,691,308,032 B（约 1.58 GiB），明确不达 4 GiB 生产规格，生产接入仍以扩容和另行授权为前置条件。

## 0.28 - 2026-09-04

- 经用户批准，当前 `shoulder-press-v2` 生产任务固定使用全帧：移除 `MOTION_ANALYSIS_SAMPLE_FPS` 运行时配置及部署样例，残留旧正数设置也不能改变 v2 采样语义。底层固定 FPS 能力仅为历史 `shoulder-press-v1` 的 5 FPS 兼容保留，不进入当前 v2 生产或验收入口。
- benchmark/smoke 收紧为只运行 `all_frames`；验收只检查人工真值 90、总耗时不超过 600 秒、峰值 RSS 低于 1.5 GiB、Swap 为零，以及 `total_count`、`count_error` 和人工真值严格自洽。报告出现 5/10 FPS 或其他额外采样档时 fail-closed。
- 代码边界收口提交为 `b422a4b`。0.26–0.27 中的三档运行记录继续作为历史验收事实保留，不再代表当前公开生产或验收流程。

## 0.29 - 2026-09-04

- 仅全帧验收进一步 fail-closed：唯一 `all_frames` 档必须显式记录 `sample_fps=null`，且解码帧数、推理帧数均为非负整数并严格相等，防止抽帧结果伪装为全帧报告。
- benchmark 函数与管理命令的 `manual_total_count` 均固定为 90：benchmark 函数在视频探测前拒绝其他值并落盘脱敏失败报告，管理命令在调用 benchmark 前拒绝其他值。对应修复提交为 `6d0c2e9`。

## 0.30 - 2026-09-04

- 固定 FPS 内部兼容路径增加异常时间戳保护：非有限解码时间戳回退为帧序号与源帧率推导值，异常巨大有限时间戳以 O(1) 跳算采样游标，避免逐间隔循环占死单 Worker；对应修复提交为 `9d497b2`。该路径仅供历史 `shoulder-press-v1` 固定 5 FPS 使用，当前 v2 生产与验收仍严格只走全帧。
- 仅全帧边界由 `b422a4b`、`e7d05e4` 收紧，证据与固定人工真值由 `6d0c2e9`、`a9ecf73`、`1d01d84` 收口。最终使用 committed HEAD `1d01d8434d11cfb1d55db9dcd9bccf79e5c37274`、归档 SHA-256 `4270b1c3a8a2d66d10e2b59b5c394f5eb81be0f9309ab3cd87d68e07d6bba222`、run_id `20260904T104350Z` 仅运行一次 `all_frames`：解码/推理均为 8,929 帧，计数 90、误差 0，耗时 430.125 秒，峰值 RSS 632,373,248 B，Swap 0，验收通过。
- 独立算法服务器正式 app 已更新为 `1d01d84`；原正式 `0df937f` 保留为 `app.previous-0df937f`，`app.previous-de56352`、v1 `app.previous-77b0166`、首次失败 v2 与全部历史脱敏报告均保留。input/tmp 为空，无 benchmark、Celery 或 Web 进程，生产 PostgreSQL、Redis 与 Celery 未接入；服务器物理内存仍约 1.58 GiB，明确不达 4 GiB 生产规格。

## 0.31 - 2026-09-05

- 批准将 `pp-mcare` 建设为独立动作分析计算服务：代码与 MotionCare 共用仓库，但分别构建并部署到不同服务器；算法端不连接业务 PostgreSQL、Redis/Celery，也不持有七牛长期密钥，只通过受控 HTTPS API 和单任务短期凭证工作。
- 训练视频绑定 `TrainingRecord` 后自动创建分析任务；`pp-mcare` 空队列每 15 分钟轮询，有积压时单任务租约、串行连续处理。医生端取消开始分析、重新分析和失败重试按钮。
- PP-TinyPose 只走全帧流式推理，锁定、计数并绘制唯一主训练者；同一份单人关键点同时进入动作插件和 PP 可视化，产出无动作文字、无音轨的私有骨架 MP4，上传并回传后清理本地缓存。
- 算法和医生共同写 `TrainingRecord` 上同一组动作结果字段；医生可以覆盖算法值。`MotionAnalysisJob` 只保留执行状态、版本、原始结果、失败原因和骨架视频元数据。当前不重跑失败算法，失败或暂不支持时由医生填写结果。

## 0.32 - 2026-09-06

- 经用户确认新增坐站转移、坐姿划船、腿部后踢三个独立插件，并接入与肩部推举相同的自动任务、骨架视频和训练结果回写流程；仅新视频绑定时建任务，不补历史、不重试。高抬腿仍不支持，肩部推举版本保持不变。
- 坐站以“站起即计一次、坐回再复位”计数，两侧独立识别取最大值；划船两侧取最大值；后踢两侧求和。人工参考分别为 12、15、左 10 右 10，视频仅用于隔离验证，不录入患者训练记录。
- 坐站和后踢采用版本化 `body-crop-v1` 原图定位后裁切细化，所有帧都处理，计数和骨架共用每帧最终姿态；帧数不等同底层模型调用数。新动作质量判定标记 `provisional_unvalidated`，单样本计数吻合不代表临床质量验证。

## 0.33 - 2026-09-07

- 按用户确认的《游戏训练项目表.xlsx》对齐小游戏难度：反应抑制 4/6/9 个数字，分类与声音 3/4/5 个选项，拼图 4/6/9 块，颜色与图案候选及序列固定 3/4/5，每项展示 2 秒并保留 500ms 过渡。
- 分类转换统一为物品类别判断，更新完整题目文案；声音保留同类干扰与唯一 soundId 正确答案。
- 医生处方端提供三档难度选择与规则说明；患者开始前可查看难度并仅降低本次训练难度，允许恢复设定。降低原因四项单选，其他可补充说明，沿用既有训练记录字段供医生查看。
- 本次覆盖旧游戏 spec 中的难度数量、展示节奏、类别/颜色切换及允许患者提高难度等约定；已在两份现有游戏设计稿追加同日决策，保留历史正文。
- 医生后台左侧移除“训练记录”菜单入口，保留训练数据及“训练与健康”记录查看。

## 0.34 - 2026-09-07

- 用户确认固定素材保留现有七牛私有空间及 `cdn.whestsun.com`，采用后端批量签名、默认有效期 10 分钟；不新增域名，不修改患者训练视频访问权限。
- 用户确认保留 `8888` 未登录演示访问；签名接口只允许版本化清单内的 18 张游戏图片和 5 段动作说明语音，不接受任意对象路径。
- 新建私有签名访问设计供用户审阅，并在 9 月 2 日包体优化 spec 中记录替代关系。本文仅记录已确认方向和待审设计，不代表签名接口、素材上传或微信 7.0.4 发布已完成。

- 用户已审阅并批准 9 月 7 日私有签名访问书面 spec；对应七任务实施计划已编制并自审，覆盖固定白名单、匿名限流、客户端缓存与取消、签名验收及两阶段发布。当前仅完成设计与计划，尚未实施签名能力或上传微信 7.0.4。

- 私有签名访问 Tasks 1–6 及最终审查修复已落地至 `0fce037`；后端 1235 项、管理端 317 项、小程序 981 项测试通过，Ruff、迁移检查、管理端 lint/build、小程序微信开发/H5 生产/微信生产构建及包体检查通过。
- 23 项固定素材已上传到既有七牛私有空间的 `motioncare/static-assets/v-3aafe09211fd/` 前缀，第二次发布均返回已存在且无冲突；本地签名配合真实 CDN 的正文完整性和匿名、篡改、过期拒绝预验通过。正式 API、患者 H5 与微信 7.0.4 的发布状态将在两阶段发布完成后另行记录。

- 2026-09-08 将 `c58d7aa` 快进发布到 `main`：第一阶段生产流水线 34181288314 成功，仅上线后端能力并沿用旧患者 H5；随后正式 API 获取的 23 项签名素材正文、媒体类型、字节数和 SHA-256 全部通过，匿名、篡改、过期访问均被拒绝。
- 正式素材变量启用后，第二阶段生产流水线 34182242505 成功，患者 H5 镜像实际重建并部署。微信开发者工具 CLI 已上传开发版 7.0.4，描述为“小游戏难度及私有素材加载优化”；尚未提交平台审核或正式上架。普通 Chrome 因绑定页既有 `Taro.login` 依赖无法完成 8888 全链路，真机图片与语音验收仍待完成。

## 0.35 - 2026-09-08

- 六款认知游戏准备页开放简单、中等、困难三档自由选择，可高于或低于指导老师设定，点击即生效且仅用于本次训练；移除原因选择、其他说明、原因必填及独立恢复入口。新训练记录继续保留兼容字段，调整原因统一为空字符串，历史记录与待补传内容不改写。
- 三档采用青绿、蓝、橙的独立未选中与选中配色，并以文字和勾选共同表达状态；三列等宽、实际点击区域在 320/375/414 CSS px 视口均不小于 44px，六组文字对比度均不低于 4.5:1。患者 H5 的三档支持 Tab 聚焦，并可用 Enter 或 Space 选择，Space 不触发页面滚动。
- 本地验证通过：后端隔离测试库 1237 项、管理端 317 项、小程序 1001 项测试通过，管理端 lint/build、小程序 H5 与微信生产构建及包体预算检查通过。微信包体实测主包 503023 字节、游戏分包 1034143 字节、总包 1537166 字节。本地 H5 真实页面完成三个视口、三种选中状态截图及实际进入困难游戏验证；本条不代表微信真机验收、提交审核、正式上架或生产发布。

### 2026-09-09 微信开发版 7.0.4 更新

- 按用户授权重新打包并上传患者自由选择难度版本，包含开始前三档自由选择、取消调整反馈及三档配色。
- 正式微信构建、包体检查通过；微信 CLI 退出 0 并返回 `✔ upload`，上传版本 `7.0.4`，总包 1597259 B。
- 本次仅微信开发版上传；未设体验版、未提审、未正式上架，未部署 H5；代码仍在隔离工作区未提交。

版本更正（2026-09-09, codex）：用户纠正本轮版本应为 7.0.5。复用同一已验证正式构建包，以 --version 7.0.5 重新上传微信开发版，CLI 退出 0 并返回 ✔ upload，上传总包 1597259 B。此前误用 7.0.4 的上传事实保留，当前最新开发版已更新为 7.0.5；未设体验版、未提审或正式上架。回执为本计划 SDD 目录 motioncare-weapp-705-free-difficulty-upload-20260909.json/.log。

### 2026-09-09 隐藏后台项目状态展示

- 根据用户确认的简短设计，移除研究项目列表、项目详情、医生处方入口及患者研究录入详情的项目状态列/字段/标签；访视归档提示改为“当前表单只读”。
- 项目完结操作及其反馈、权限和只读规则、处方与访视自身状态保持不变。
- 前端 317 项、后端 1237 项测试通过；前端 lint 0 错误/5 条既有警告，构建通过（保留既有大 chunk 提示），独立审查通过。本地演示数据下五处实页无项目状态外显、无横向溢出。
- 本次后台改动尚未提交、推送或部署。

### 2026-09-09 纠正椰林步道模拟的 AI 监督标记

- 用户确认“椰林步道模拟（原地高抬腿+摆臂）”不支持 AI 监督。新增处方数据迁移 0013，精确纠正 motion-aerobic-high-knee 的动作库与既有处方快照能力标记；保留教学视频、普通跟练和其他动作能力。实际算法能力清单此前已不支持此动作。
- 新初始化标记断言先失败（True 与 False 不符）；全量后端 1237 项通过、1 条旧能力断言失败，更新该测试后受影响两文件 36 项通过。前端 317 项通过，lint 0 错误/5 条既有警告，构建与迁移完整性检查通过；独立审查和限定复审通过。
- 本地演示卡片已验收“无 AI 监督”。本次尚未提交或部署；线上需部署并执行迁移后生效。

### 2026-09-09 单患者训练明细导出设计

- 用户确认单患者导出范围、逐题有效作答口径，纳入运动结果及心率/血压/血氧；批准先撰写完整书面设计。
- 新 spec 为 review：五工作表 Excel、规范逐题采集与幂等、历史计时说明、单项目权限及既有固定健康观察窗口。书面设计待用户确认，尚未编写新实施计划或实现导出。


## 2026-09-09 — 单患者训练明细导出书面设计确认与实施计划

- 用户批准 `docs/superpowers/specs/2026-09-09-patient-training-detail-export-design.md`，状态更新为 approved。
- 新增同日 `patient-training-detail-export` 实施计划，分八任务覆盖逐题规范与历史兼容、会话幂等、六游戏计时、五表导出、权限审计、Web 入口及跨端验收；计划当前为 review。
- 本次仅完成设计/计划文档，功能尚未实施，未提交、未部署、未上传新的小程序版本。

### 2026-09-09 单患者训练明细导出实施与跨端验收

- 本轮承接逐题数据基础：新增规范题关系、UUID语义幂等、旧payload在线兼容及历史解析迁移；六游戏客户端记录排除非作答阶段的有效毫秒并保留自由选难和稳定补传。旧长期统计计划保留原状态，未扩展为已完成。
- 医生端单患者“训练与健康”新增按单项目及日期范围导出入口，后端以授权快照生成训练场次、游戏逐题、运动明细、训练期间生理数据、字段说明五表，含历史处方、显式文本安全、长JSON拆列、容量与截止保护及导出审计。运动仍按首次录像开始＋处方秒数＋300秒闭区间关联生理数据。
- 新版有效与legacy历史毫秒分列；历史仅恢复来源中已有合法字段，不能补齐未采集的有效耗时、交换次数或绝对起止时间。游戏本期无独立生理窗口；无AI高抬腿允许保留录像及医生结果。既有难度、项目状态隐藏及高抬腿能力纠正改动保留。
- 新增5项跨端验收通过：真实患者API写入→医生Session/CSRF导出；样本11场/14题/3运动/12生理关联，全列类型/计时/长JSON核对，主控完成五表七区域渲染与目视、实际浏览器下载及字节一致性检查。合成样本位于 `output/training-detail-export/sample.xlsx`；全部测试样本只写PID独立测试库，无患者业务库写入。
- 迁移演练由training 0015升到当前0018：scanned=1 created=2 skipped=2，原JSON未变；makemigrations完整性检查无漏迁移。容量实测10000场/20000数据行准备302次SQL、4.75秒，10001场拒绝；真实单表200000/总500000行边界拒绝溢出并清理。
- Web同版336测试、lint与构建已通过（5项既有lint警告和既有chunk提示）；小程序同版1035测试、微信/H5生产构建及微信包体检查已通过，203文件哈希复核无变化。小程序额外全量tsc仍有既有错误，单调时钟仅微信开发工具验证，不声称真机验证通过。
- 最终后端全量1446项通过（224.89秒、退出0），本轮证据见 `.superpowers/sdd/2026-09-09-patient-training-detail-export/task-8-report.md`；独立收口审查待主控执行。当前实施基线为隔离区未提交改动，未提交、推送、部署或上传。后续发布顺序固定为后端兼容写入与迁移→核对历史迁移→Web入口→新版小程序；需新的明确发布授权及小程序版本号。

### 2026-09-09 单患者训练明细导出最终审查收口

- 八任务独立双轴审查与整体审查完成；整体审查发现合法数值 confidence_level 被质量白名单遗漏，已兼容有限数值（含0）及原字符串，并保持非法值和敏感字段过滤；限定复核通过，无遗留问题。
- 最终修复后后端全量1448项通过（217.63秒），相关导出行/实际工作簿31项通过；Web336项、小程序1035项及已有同版构建证据不变。此前1446为修复前时点，最新结果见 `.superpowers/sdd/2026-09-09-patient-training-detail-export/backend-postfix-evidence.md`。
- 合成五表样本与界面验收产物保留本地；设计/计划更新为implemented。分支 codex/game-difficulty-704 及本地审查记录保留；尚未提交、推送、合并、部署或上传，新功能需后续发布授权后才能在线使用。

### 2026-09-09 逐题导出仅展示有效作答耗时

- 用户明确取消历史耗时展示；逐题导出改为仅包含可确认的有效耗时及正确/错误/超时结果，删除历史耗时列、历史计时题数与旧墙钟口径说明。原始历史数据保留后台，其他合法训练明细继续导出。
- 同步修订现有设计和计划覆盖说明，当前调整实施中，尚未发布。

### 2026-09-09 仅有效作答耗时调整验收完成

- 历史耗时列与旧题已从导出移除；每题耗时仅接受规范采集的0–3600000毫秒整数，正确/错误/超时与真实0保留。明细完整性核对连续题号和已存完成题数，证据不足不重算场次摘要。
- 94项受影响测试、Ruff及独立双轴复核通过；新版样本12条有效题、11列，字段与格式已实际验收。原始历史存储及其他合法训练明细保留，未提交、未部署。

### 2026-09-09 游戏数据按交互粒度调整

- 用户明确顺序游戏需每一步耗时和对错，并确认退出/超时保留已选择步骤；单次选择按题，拼图新增点击次数与每张总有效耗时。新设计承接已完成导出，扩展六表并保留旧版本真实数据，不补造未采集明细。当前实施中，未发布。

### 2026-09-09 游戏逐步选择与未完成保留验收完成

- 顺序游戏每步记录所选/正确内容、有效耗时和判定；超时或退出保留已选步骤，题目标记未完成；单次选择按题保留结果和耗时；已完成拼图新增真实点击次数，独立保留交换次数与总有效耗时。
- 新v2父子数据原子保存及UUID幂等；六表导出新增顺序选择明细，历史未采集值不补造，无可靠耗时旧题不展示。兼容旧缓存的多场补传队列保证新旧UUID独立保留及处理。
- 后端1517、小程序1057、Web336项全量通过；审查修复后另有导出行54项和小程序159项定向通过，最终微信/H5构建、微信包体与迁移只读检查通过。独立审查两项发现均已复审解决；六表真实API合成样本与浏览器下载/视觉通过。
- 全部变更仅在codex/game-difficulty-704隔离工作区，未提交、未部署、未执行业务库迁移或小程序上传；上线后新训练才会采集新增明细。

### 2026-09-09 用户要求暂时精简训练明细导出列

- 移除全部工作表中的起止时间可用性、游戏得分、逐题完整性、备注（含分段）、窗口口径、三项生理数据可用性、数据口径说明及全部动作质量详情分段；同步移除对应字段定义。原始数据和统计计算保留，不影响每步/每题有效耗时、对错、拼图点击或运动次数。仅本地修改，未发布。

导出列精简验收（2026-09-10）：工作簿/真实API验收17项、导出API23项通过，Ruff与差异空白检查通过，独立限定复核无阻塞。精简样本六表列数50/14/12/20/11/3，字段说明121行；指定列及其分段和定义均已移除，实际样本渲染无错位。未提交或发布。
