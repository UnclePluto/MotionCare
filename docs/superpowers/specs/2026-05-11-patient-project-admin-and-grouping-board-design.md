# 患者档案、研究项目与分组看板（管理端）设计

日期：2026-05-11  
范围：MotionCare 管理端（Django REST + React/Ant Design），聚焦患者域、研究项目列表/详情、分组与批次确认流程的交互与规则。  
**已选交付策略**：纵向按域切片（患者 → 项目列表 → 项目详情看板），看板切片启动前对关键接口做一轮轻量契约对齐。

## 背景

当前能力缺口主要包括：患者档案缺少编辑与删除（及停用）闭环；患者缺少直接加入多个研究项目的入口；项目缺少删除能力；项目列表展示 CRF 模板列与入口分散；项目详情以多 Tab 承载分组、患者、随机分组与 CRF，与「以看板统一分配与草稿调整」的目标不一致。  
后端已具备部分 REST 能力（如 `PatientViewSet`、`StudyProjectViewSet` 等），本设计以前端与规则补齐为主，并按需增补接口与校验。

## 目标与非目标

### 目标（本期）

1. **患者档案**  
   - 列表行内与详情页均可进入**编辑**。  
   - **删除/停用等高风险操作**仅在患者详情提供。  
   - **删除规则**：若仍存在项目关联（`ProjectPatient`）或按产品策略认定的 CRF 相关数据，则**禁止物理删除**；引导停用或先解除全部项目关联。  
   - **加入研究项目**：支持一次**多选多个项目**；选择器中**已在该项目下的记录置灰且不可勾选**；展示提示：加入后将**按各项目当前分组权重（`target_ratio`）自动分配实验组**（各项目独立计算）。  
   - **患者详情**：展示该患者参与的研究项目列表；**每一行提供「打开 CRF」**（对应该 `ProjectPatient`，与 `/crf?projectPatientId=` 一致）。

2. **研究项目列表**  
   - **隐藏**「CRF 模板」列（不在列表暴露模板版本）。  
   - 提供 **「详情」** 与 **「删除」**；删除前二次确认。  
   - **删除规则**：若存在任意 `ProjectPatient`，或存在**状态为待确认（pending）**且仍阻塞删除的分组批次（与现有 `GroupingBatch` / `ProjectPatient.grouping_status` 规则对齐，实现时以代码与单测为准），则**禁止删除**并返回明确错误文案。

3. **项目详情（看板化）**  
   - **保留 `GroupingBatch` 模型与「待确认 / 确认」语义**：从患者池勾选多人 → 生成（或进入）**待确认**批次；列内展示与拖拽仅修改**草稿**分组；用户**显式确认**后写入最终状态（与现有 `confirm` 等流程对齐）。  
   - **患者池**：面向**全库患者**；以「尚未加入本项目」为主要操作对象；**已加入本项目**者在池中**弱化显示或过滤**（实现可选，默认与「不可重复勾选」一致）。  
   - **列**：每组一列（竖队列）；列头为**组名**与**占比**；多列占比**展示合计 100%**，并与后端 **`target_ratio` 整数权重**双向一致（见「占比与尾差」）。  
   - **列内卡片**：简略信息含**姓名、性别、手机尾号**；支持**跨列拖拽**调整草稿分组；悬浮露出**患者详情**与**从当前批次/队列移除（取消）**等操作（具体动词与 API 字段在实现计划中落到端点）。  
   - **原项目详情中的 CRF 主入口**：不再作为 CRF 主路径（与「CRF 自患者侧进入」一致）；可移除原 CRF Tab 或降级为次要链接，**默认移除主 Tab**，避免与信息架构冲突。

4. **占比与尾差**  
   - **不改库表语义**：持久化仍为各组 `target_ratio`（正整数）。  
   - 列头以权重换算为**整数百分比**展示，须处理**舍入尾差**（例：权重 1:1:1 → 展示 **33%、33%、33%**，不展示无限小数）。  
   - 用户在看板修改占比时，前端换算回一组整数权重，**PATCH** 更新各组；须定义**溢出保护**与写回后仍满足随机算法输入约束（细则在实现阶段用单测锁死）。

5. **随访与 CRF**  
   - **本期不接**「从随访流程进入 CRF」的页面与路由。  
   - 在患者详情等位置可放置**简短占位说明**（例如未来将在此关联访视与 CRF）。

### 非目标（本期不做）

- 随访/访视 UI 与 CRF 的深度串联。  
- 移动端看板专项优化（可按桌面优先实现，窄屏降级策略另议）。  
- 与分组无关的报表、权限模型大改（沿用现有认证与视图集前提，除非实现时发现硬缺口）。

## 方案选择（已确认）

采用 **纵向按域切片** 为主：

1. **切片一**：患者（编辑、详情侧删除/停用规则、多项目加入、详情项目表 + 每行 CRF）。  
2. **切片二**：项目列表（隐藏 CRF 列、详情与删除及后端校验对齐）。  
3. **切片三**：项目详情看板（池、批次、列头占比、拖拽、确认）；**启动前**对池查询、比例 PATCH、批量入组、批次确认等做**轻量契约对齐**（小范围 API/序列化器评审即可）。

## 关键决策汇总

| 主题 | 决策 |
|------|------|
| 患者物理删除 | 存在项目关联或认定 CRF 数据则禁止；否则允许；提供停用路径 |
| 项目删除 | 有项目患者或未闭环分组流程则禁止 |
| 批次与确认 | 保留 `GroupingBatch`；看板为草稿与确认聚合 UI |
| 患者池范围 | 全库；已入本项目者弱化或过滤 |
| 占比存储 | 仍为 `target_ratio`；列头为展示换算 + 尾差规则 |
| 批量加入项目 | 多选；已加入项目在选择器中置灰不可选；提示按比例自动分组 |
| CRF 主路径 | 患者详情按项目分行 + 每行打开 CRF；项目详情弱化/移除 CRF 主 Tab |
| 编辑入口 | 列表与详情均可编辑；删除仅详情 |
| 随访 | 下一迭代；本期占位文案 |
| PostgreSQL 与锁 | 延续已修复的 `confirm` 路径：`select_for_update(of=("self",))`，避免可空外键与 `select_related` 组合导致的外连接加锁错误 |

## 信息架构与路由（概念）

- **患者列表**：行内「编辑」「详情」等；「加入项目」可放列表或详情（实现计划定具体位置，须满足多选项目交互）。  
- **患者详情**：编辑、删除/停用、加入项目、参与项目表（每行 CRF）、随访占位说明。  
- **项目列表**：列含项目名称、状态、患者数（若后端已有或后续补全）等；**不含** CRF 模板列；操作「详情」「删除」。  
- **项目详情**：单页看板为主；分组「元数据」配置（组名、启用、排序等）通过**抽屉或次级面板**保持可达，不丢失现有 `StudyGroup` 管理能力。

## 看板交互（概念级）

1. **患者池区域**：搜索/筛选；多选患者 → 触发「进入批次」或等价动作，调用现有或封装后的「创建待确认批次」能力。  
2. **分组列区域**：每列对应 `StudyGroup`；列头编辑占比（联动 100% 展示）；列内为卡片列表，支持拖拽跨列。  
3. **卡片操作**：进入患者详情路由；从当前草稿/批次移除须与后端状态一致（待确认 vs 已确认约束在实现计划中写清）。  
4. **确认条**：显式「确认分组」触发既有确认流程；成功后 UI 与池内置灰规则与业务状态一致。

## 错误处理与幂等

- **批量加入**：前端置灰降低冲突；服务端仍依赖 `(project, patient)` 唯一约束，冲突时返回可读错误，避免静默数据异常。  
- **删除患者/项目**：使用明确 HTTP 状态与 `detail` 文案，前端在 Modal 中展示后端原因。  
- **批次确认**：保持事务与行级锁策略与已修复实现一致，避免 `FOR UPDATE` 作用于外连接可空侧。

## 测试与验收要点

- 患者：编辑保存、禁止删除场景、多选加入与置灰、详情多行 CRF 跳转。  
- 项目：列表无 CRF 列、删除二次确认、禁止删除场景。  
- 看板：占比展示尾差、写回 `target_ratio`、创建批次、拖拽草稿、确认前后状态、回归 `grouping-batches/.../confirm/`。  
- 后端单测：删除保护、比例换算与舍入、批次确认锁与查询。

## 修订记录

- 2026-05-11：初稿经产品确认；交付策略定为纵向切片一。
